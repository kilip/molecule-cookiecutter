---
name: Testing
on:  # yamllint disable-line rule:truthy
  pull_request:
    branches:
      - main
      - next
  push:
    branches:
      - main

jobs:
  testing:
    uses: kilip/gh-common/.github/workflows/ansible-testing.yml@wip
    with:
      dists: ubuntu-22.04
