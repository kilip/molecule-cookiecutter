---
name: Release

on:  # yamllint disable-line rule:truthy
  workflow_run:
    workflows: ["Testing"]
    branches: ["main"]
    types:
      - completed

jobs:
  release:
    name: Create Release
    uses: kilip/gh-workflows/.github/workflows/ansible-release.yml@main
    secrets: inherit
    permissions:
      contents: write
